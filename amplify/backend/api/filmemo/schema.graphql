type Bookmark
  @model
  @auth(rules: [{allow: owner}])
  @key(name: "ByImdbID", fields: ["imdbID"], queryField: "bookmarksByImdbID")
{
  id: ID!
  imdbID: String!
  title: String!
  posterURL: String!
}
